<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.lease.web.app.mapper.LabelInfoMapper">

    <select id="getLabelInfoByApartmentId" resultType="com.atguigu.lease.model.entity.LabelInfo"
            parameterType="java.lang.Long">
        select id, type, name
        from label_info
        where is_deleted = 0
          and id in (
            (select label_id
             from apartment_label
             where is_deleted = 0
               and apartment_id = #{apartmentId}))
    </select>
    <select id="getLabelInfoByRoomtId" resultType="com.atguigu.lease.model.entity.LabelInfo"
            parameterType="java.lang.Long">
        select id, type, name
        from label_info
        where is_deleted = 0
          and id in (select label_id
                     from room_label
                     where is_deleted = 0
                       and room_id = #{id})
    </select>
</mapper>
